# KeepingUpAppearances

A baseline program made with jupyter notebook that takes a photo, turns it to black and white and then tries adding colours to match the original photo.

Works with one photo at a time.
Epochs value works best around 1000.

See some of the photos to see model's performance.

**Required Libraries**:
keras
skimage
numpy
tensorflow

To install
> pip install keras skimage numpy tenserflow
